# D10 - Alias y prefijos

Hoy evitas la columna ambigua. Cuando dos tablas tienen `Id`, `Nombre` o `Ciudad`, usa alias como `c.Nombre` y `p.Nombre` para indicar el origen.

Al dormir debes poder crear alias claros, seleccionar columnas repetidas y leer consultas con varias tablas.