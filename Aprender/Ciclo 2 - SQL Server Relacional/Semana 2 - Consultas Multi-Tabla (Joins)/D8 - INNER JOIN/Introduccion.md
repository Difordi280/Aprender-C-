# D8 - INNER JOIN

Hoy combinas tablas por coincidencias exactas. `INNER JOIN` es exclusivo: si una fila no encuentra pareja bajo `ON`, desaparece del resultado.

Al dormir debes poder escribir el `ON`, predecir qué filas quedan y diferenciar un join de un producto cartesiano.