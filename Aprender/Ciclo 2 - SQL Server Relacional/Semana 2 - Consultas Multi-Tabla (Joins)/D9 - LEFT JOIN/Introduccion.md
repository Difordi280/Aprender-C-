# D9 - LEFT JOIN

Hoy conservas todos los registros de la tabla izquierda. Si no hay coincidencia a la derecha, SQL Server rellena sus columnas con `NULL`.

Al dormir debes poder listar clientes sin facturas y explicar cuándo elegir `LEFT JOIN` en lugar de `INNER JOIN`.